# Photo Manager Configuration

# Photo Storage Paths
PHOTOS_PATH=/photos
DATABASE_PATH=/app/data/metadata/photos.db
THUMBNAIL_PATH=/app/data/cache/thumbnails
# Note: Recycle bin uses soft-delete (deleted_at timestamp), no physical directory needed

# Application Behavior
SCAN_ON_STARTUP=true
AUTO_WATCH=true

# Performance Settings
THUMBNAIL_SIZE=200
THUMBNAIL_QUALITY=80

# OIDC Authentication (Required for production)
# Leave empty to disable authentication (not recommended for production)
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_DISCOVERY_URL=https://your-oidc-server/.well-known/openid-configuration
OIDC_REDIRECT_URI=http://localhost:8000/auth/callback

# Session Security
# Generate a secure random key for production (e.g., using: python -c "import secrets; print(secrets.token_hex(32))")
SESSION_SECRET=change-this-to-a-random-secret-key

# Development Mode (INSECURE - Only for development/testing)
# Set to true to bypass authentication during development
# DO NOT use in production!
DEV_MODE=false
